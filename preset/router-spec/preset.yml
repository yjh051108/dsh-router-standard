name: Router Spec (experimental)
description: Task-aware routing — deep-think-first (spec): classified persona + full prompt sections; the long first-turn reasoning chain is the point. Auto-matches persona by model (Pro/Flash) in weak internal-routing mode. Full Standard tools after the first tool call.
