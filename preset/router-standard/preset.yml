name: Router Standard (experimental)
description: Task-aware routing — RL-interface restoration: one-sentence persona + shell/editor surface; think-act feedback loops. Auto-matches persona by model (Pro/Flash) in weak internal-routing mode. Full Standard tools after the first tool call.
